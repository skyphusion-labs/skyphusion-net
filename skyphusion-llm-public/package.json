{
  "name": "skyphusion-llm-public",
  "version": "0.12.0",
  "description": "Multimodal AI on Cloudflare Workers: chat (39 models across 6 providers), image / TTS / STT / video / music generation, RAG over PDF and XLSX, multi-turn conversations, behind Cloudflare Access.",
  "license": "AGPL-3.0-only",
  "author": "Conrad Rockenhaus",
  "homepage": "https://github.com/SkyPhusion/skyphusion-llm-public",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SkyPhusion/skyphusion-llm-public.git"
  },
  "bugs": {
    "url": "https://github.com/SkyPhusion/skyphusion-llm-public/issues"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap": "node -e \"const fs=require('fs');if(fs.existsSync('wrangler.toml')){console.error('wrangler.toml already exists; not overwriting.');process.exit(1)}fs.copyFileSync('wrangler.example.toml','wrangler.toml');console.log('Created wrangler.toml from wrangler.example.toml. Fill in database_id (and worker name if you want a custom one) before deploying.')\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:create": "wrangler d1 create skyphusion-llm-public",
    "db:migrate:local": "wrangler d1 execute skyphusion-llm-public --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute skyphusion-llm-public --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "aws4fetch": "^1.0.18",
    "unpdf": "^1.6.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  }
}
